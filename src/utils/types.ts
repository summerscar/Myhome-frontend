export interface headerConfig {
  color: string,
  blur: string
}

export interface bgConfig {
  image: string,
  opacity: string,
  blur: string
}

export interface room {
  icon: string,
  name: string
}
